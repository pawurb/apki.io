middleman build
echo "Build completed"
git add build/ -A
git commit -m 'new build'
# in caseo of conlicts
## git push origin `git subtree split --prefix build development`:master --force
git subtree push --prefix build origin master
echo "Deploy completed"

